node_modules/
dist/
*.log

# InstaCloud: the project link, agent session and observe-hook state (per machine)
.insta/

# Agent skills that `insta project create` / `npx skills add` install (regenerable); keep the repo's own
.claude/skills/*
!.claude/skills/pr-shepherd-setup
.agents/skills/*
!.agents/skills/pr-shepherd-setup
.github/skills/
skills-lock.json

# Local config and credentials (templates: config.example.yaml, deploy.env.example), e.g. a dev
# environment's config.dev.yaml + deploy.dev.env
config.yaml
config.*.yaml
!config.example.yaml
deploy.env
deploy.*.env
.env
*.backup

# Generated by scripts/slack-manifest.sh -o
slack-app-manifest.yaml
